# Brainstorming Skill Routing Simplification Plan

## Scope
- Update `skills/brainstorming/SKILL.md`
- Do not change `skills/writing-plans/SKILL.md`

## Steps
1. Remove the post-checklist hard gate that forces `skill-creator` vs `writing-plans` selection.
2. Shorten the terminal-state paragraph so it ends at the default `writing-plans` transition.
3. Remove the user-review-gate hard gate that requires both skill-routing options.
4. Remove the implementation bullet that repeats the skill-routing branch.
5. Verify that `skills/writing-plans/SKILL.md` still contains the skill-specific routing rules.
6. Sync the repo copy to installed plugin directories.
