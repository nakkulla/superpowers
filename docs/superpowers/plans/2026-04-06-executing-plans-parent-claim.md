# Executing Plans Parent Claim Plan

## Scope
- Update `skills/executing-plans/SKILL.md`
- Do not change `skills/finishing-a-development-branch/SKILL.md`
- Do not change `bd-execute`

## Steps
1. Add a Step 1 bullet that claims the linked parent bead when Beads integration is `Full` or `Parent only`.
2. Keep the existing child resume/seed logic under `Beads Full`.
3. Update the `Parent only mode` explanation so it says the parent is claimed once and children are not tracked.
4. Expand the `bd dolt push` wording so `Parent only` also preserves the parent claim at completion and interruption.
5. Update interrupted-execution wording so the preserved state description includes the parent bead as well as child beads.
6. Verify that child claim/resolve lines remain unchanged.
7. Sync the repo copy to installed plugin directories.
